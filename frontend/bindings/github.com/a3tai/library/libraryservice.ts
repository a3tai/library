// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as library$0 from "./internal/library/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * BookPassages returns passages for the reader, in chunk_index order.
 * limit is clamped server-side to 1000.
 */
export function BookPassages(bookID: string, offset: number, limit: number): $CancellablePromise<library$0.Passage[]> {
    return $Call.ByID(159523870, bookID, offset, limit).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * BookTOC returns the table of contents derived from the stored passages.
 * May be empty for plain-text imports that have no chapter labels. Cached
 * per book; the cache is invalidated on re-index.
 */
export function BookTOC(bookID: string): $CancellablePromise<library$0.TOCEntry[]> {
    return $Call.ByID(624892855, bookID).then(($result: any) => {
        return $$createType3($result);
    });
}

/**
 * CancelImport requests the running import to stop. Returns true if a
 * cancel signal was sent.
 */
export function CancelImport(): $CancellablePromise<boolean> {
    return $Call.ByID(1220401929);
}

/**
 * ChatAvailable lets the frontend probe whether LM Studio is reachable
 * without launching a turn.
 */
export function ChatAvailable(): $CancellablePromise<boolean> {
    return $Call.ByID(852091515);
}

/**
 * ChatTurn runs one round of conversation. The model is given a
 * book-grounded system prompt and access to the library's MCP tools; if it
 * emits tool_calls, we execute them in-process via the MCP server and feed
 * the results back, looping up to a small bound to prevent runaways.
 */
export function ChatTurn(req: $models.ChatRequest): $CancellablePromise<$models.ChatResponse> {
    return $Call.ByID(1723126079, req).then(($result: any) => {
        return $$createType4($result);
    });
}

/**
 * EnrichmentQueueDepth exposes the pending-lookup count for the UI.
 */
export function EnrichmentQueueDepth(): $CancellablePromise<number> {
    return $Call.ByID(2110332843);
}

export function GetBook(id: string): $CancellablePromise<library$0.Book> {
    return $Call.ByID(735869103, id).then(($result: any) => {
        return $$createType5($result);
    });
}

export function GetPassage(id: string): $CancellablePromise<library$0.Passage> {
    return $Call.ByID(2887767514, id).then(($result: any) => {
        return $$createType0($result);
    });
}

/**
 * HydrateMetadata enqueues up to `limit` books that still need metadata into
 * the throttled enricher and returns immediately with the number queued.
 * Actual lookups happen in the background at a polite rate (~1.4 RPS) so
 * large libraries don't hammer Open Library / Google Books.
 */
export function HydrateMetadata(limit: number): $CancellablePromise<number> {
    return $Call.ByID(503191664, limit);
}

/**
 * ImportPath kicks off (or queues) a non-blocking import of the file or
 * directory at path. If no import is currently running, it starts one
 * immediately. If another import is already in flight, path is appended
 * to a FIFO queue and processed once the current run finishes — that's
 * the contract behind the Add Books button never being disabled. Poll
 * ImporterStatus() for live progress; QueuedPaths lists the pending tail.
 */
export function ImportPath(path: string): $CancellablePromise<$models.ImporterStatus> {
    return $Call.ByID(3075530212, path).then(($result: any) => {
        return $$createType6($result);
    });
}

/**
 * ImporterStatus returns a snapshot of the current importer state.
 * Safe to call any time; cheap enough to poll at ~2 Hz.
 */
export function ImporterStatus(): $CancellablePromise<$models.ImporterStatus> {
    return $Call.ByID(143193154).then(($result: any) => {
        return $$createType6($result);
    });
}

/**
 * ListAuthors returns up to `limit` authors, sorted by count desc.
 * Used by the "By Author" sidebar view.
 */
export function ListAuthors(limit: number): $CancellablePromise<library$0.AggregateGroup[]> {
    return $Call.ByID(2131485754, limit).then(($result: any) => {
        return $$createType8($result);
    });
}

export function ListBooks(query: string, limit: number, offset: number): $CancellablePromise<library$0.Book[]> {
    return $Call.ByID(2757075636, query, limit, offset).then(($result: any) => {
        return $$createType9($result);
    });
}

/**
 * ListCategories returns the most-used categories for the "Categories" sidebar
 * view. Sourced from the book_categories join table.
 */
export function ListCategories(limit: number): $CancellablePromise<library$0.AggregateGroup[]> {
    return $Call.ByID(768881954, limit).then(($result: any) => {
        return $$createType8($result);
    });
}

/**
 * ListLMStudioModels returns the model ids advertised by /v1/models so the
 * settings panel can populate model dropdowns. Returns an empty slice (not
 * an error) when the endpoint isn't reachable so the UI can degrade to a
 * free-form text field cleanly.
 */
export function ListLMStudioModels(): $CancellablePromise<string[]> {
    return $Call.ByID(1902352401).then(($result: any) => {
        return $$createType10($result);
    });
}

/**
 * ListRecentlyAdded returns the most recently created books — the
 * "Recently Added" sidebar view. Ordered by created_at DESC.
 */
export function ListRecentlyAdded(limit: number, offset: number): $CancellablePromise<library$0.Book[]> {
    return $Call.ByID(1118374136, limit, offset).then(($result: any) => {
        return $$createType9($result);
    });
}

/**
 * ListSubjects returns up to `limit` subjects parsed from book metadata.
 * Used by the "By Subject" sidebar view.
 */
export function ListSubjects(limit: number): $CancellablePromise<library$0.AggregateGroup[]> {
    return $Call.ByID(1227367879, limit).then(($result: any) => {
        return $$createType8($result);
    });
}

/**
 * ListUnprocessed returns books that still need metadata or indexing —
 * the "Unprocessed" sidebar view.
 */
export function ListUnprocessed(limit: number, offset: number): $CancellablePromise<library$0.Book[]> {
    return $Call.ByID(506193239, limit, offset).then(($result: any) => {
        return $$createType9($result);
    });
}

export function MCPStatus(): $CancellablePromise<$models.MCPStatus> {
    return $Call.ByID(2420739236).then(($result: any) => {
        return $$createType11($result);
    });
}

/**
 * PickImportPath opens the native file/folder picker and returns the
 * chosen path. Returns an empty string (no error) when the user cancels.
 * 
 * macOS 26 + Wails v3 alpha workaround: Wails' own NSOpenPanel path
 * (attached or free-standing) consistently failed to surface a panel
 * on macOS 26 Tahoe with Wails alpha.74. The C call returned but no
 * panel ever appeared, leaving Go blocked on the response channel
 * forever. We bypass that by default with `osascript`, which uses
 * Finder's documented dialog scripting to present the picker.
 * 
 * Set LIBRARY_PICKER=native to try the Wails NSOpenPanel path again —
 * useful for verifying whether a Wails upgrade has fixed the regression.
 * Anything else (or unset) keeps the osascript safety net.
 */
export function PickImportPath(): $CancellablePromise<string> {
    return $Call.ByID(3456029013);
}

/**
 * RequestMetadata enqueues a single book for *priority* enrichment — used
 * when the user has just opened that book's detail view. It jumps ahead of
 * any background backfill in flight. Returns immediately; results land in
 * the books table when (and if) the lookup succeeds.
 */
export function RequestMetadata(bookID: string): $CancellablePromise<boolean> {
    return $Call.ByID(4224399774, bookID);
}

/**
 * Search runs a unified search across books and passages, plus a quick
 * substring scan over cached author/subject aggregations. Each list is
 * independently capped — callers can use the counts directly without
 * pagination for the initial preview, then drill in via per-type calls.
 */
export function Search(query: string): $CancellablePromise<$models.SearchResults> {
    return $Call.ByID(2206755262, query).then(($result: any) => {
        return $$createType12($result);
    });
}

/**
 * SearchPassages prefers semantic (vector) search when an embedding model
 * is reachable and the DB has at least one vector under that model. Falls
 * back to the FTS5 path otherwise so the feature degrades gracefully on
 * fresh libraries or when LM Studio isn't running.
 */
export function SearchPassages(query: string, bookID: string, limit: number): $CancellablePromise<library$0.Passage[]> {
    return $Call.ByID(377481695, query, bookID, limit).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * Settings returns the current effective configuration for the settings
 * panel. Stored values win; env vars + built-in defaults fill any blanks.
 */
export function Settings(): $CancellablePromise<$models.Settings> {
    return $Call.ByID(90156955).then(($result: any) => {
        return $$createType13($result);
    });
}

export function Snapshot(query: string): $CancellablePromise<$models.LibrarySnapshot> {
    return $Call.ByID(3517547282, query).then(($result: any) => {
        return $$createType14($result);
    });
}

export function StartMCPServer(port: number): $CancellablePromise<$models.MCPStatus> {
    return $Call.ByID(945277821, port).then(($result: any) => {
        return $$createType11($result);
    });
}

export function Stats(): $CancellablePromise<library$0.Stats> {
    return $Call.ByID(2363876853).then(($result: any) => {
        return $$createType15($result);
    });
}

export function StopMCPServer(): $CancellablePromise<$models.MCPStatus> {
    return $Call.ByID(1441340869).then(($result: any) => {
        return $$createType11($result);
    });
}

/**
 * TestLMStudio probes the configured endpoint with /v1/models. Returns the
 * model id list on success so the settings panel can confirm what LM Studio
 * is actually serving.
 */
export function TestLMStudio(): $CancellablePromise<boolean> {
    return $Call.ByID(3154295143);
}

/**
 * ToggleMaximise toggles the current window's maximised state. Wired to the
 * title-bar double-click on the frontend so we mimic the macOS default
 * "double-click to zoom" gesture even though the title bar is rendered by
 * the webview.
 */
export function ToggleMaximise(): $CancellablePromise<void> {
    return $Call.ByID(592587953);
}

/**
 * UpdateSettings writes the supplied values into the settings table and
 * rebuilds the LM Studio clients atomically. Empty string values fall back
 * to env + defaults (i.e. they clear any user-set override). Returns the
 * new effective Settings.
 */
export function UpdateSettings(input: $models.SettingsInput): $CancellablePromise<$models.Settings> {
    return $Call.ByID(241793514, input).then(($result: any) => {
        return $$createType13($result);
    });
}

/**
 * Version returns the AppVersion constant. Bound to the frontend so the
 * sidebar footer can render it without a hard-coded JS copy.
 */
export function Version(): $CancellablePromise<string> {
    return $Call.ByID(1255107570);
}

// Private type creation functions
const $$createType0 = library$0.Passage.createFrom;
const $$createType1 = $Create.Array($$createType0);
const $$createType2 = library$0.TOCEntry.createFrom;
const $$createType3 = $Create.Array($$createType2);
const $$createType4 = $models.ChatResponse.createFrom;
const $$createType5 = library$0.Book.createFrom;
const $$createType6 = $models.ImporterStatus.createFrom;
const $$createType7 = library$0.AggregateGroup.createFrom;
const $$createType8 = $Create.Array($$createType7);
const $$createType9 = $Create.Array($$createType5);
const $$createType10 = $Create.Array($Create.Any);
const $$createType11 = $models.MCPStatus.createFrom;
const $$createType12 = $models.SearchResults.createFrom;
const $$createType13 = $models.Settings.createFrom;
const $$createType14 = $models.LibrarySnapshot.createFrom;
const $$createType15 = library$0.Stats.createFrom;
